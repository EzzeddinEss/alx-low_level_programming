#include "main.h"
/**
 * _isupper - a function that checks for uppercase character.
 * @c: the checking parameter
 * Return: 1 if the charecter is an uppercase otherwise 0.
 */

int _isupper(int c)
{
	if (c = 'A')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
