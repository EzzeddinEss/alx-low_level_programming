#include "main.h"
/**
 * mul -  multiplies two integers and returns the result
 * @a: the first integer
 * @b: the second integer
 * Return: return the result
 */

int mul(int a, int b)
{
        return (a * b);
}
