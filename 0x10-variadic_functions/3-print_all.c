#include "variadic_functions.h"
#include <stdio.h>
#include <stdarg.h>

/**
 * print_all - a function that print diffrent types of varibles.
 * @format: is the list of args passed to the function.
 *
 * Return: Nothing.
 */

void print_all(const char * const format, ...)
{
int i = 0;
chat *str;

va_list varlist;
va_start(varlist, format)
  while (i < n)
    {
      
    }


va_end(varlist);
  
}
