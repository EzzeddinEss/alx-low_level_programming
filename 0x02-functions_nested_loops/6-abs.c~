#include "main.h"
/**
 *  _abs - computes the absolute value of an integer.
 * Return: 0
 * @x: the integer that calculate the abs 
*/

int _abs(int x)
{
	if (x >= 0)
		return (x);
	else
		return (-x);
}
