#include "main.h"
/**
 * _islower - check if the charcter is a lower case
 *
 * Return: 1 if true, else return 0.
 *
 * c: isthe checking parameterr 
*/

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
		return (1);
	else
		return (0);
}
