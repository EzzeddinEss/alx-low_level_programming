#include "main.h"
/**
 * add - a function that adds two integers and returns the result
 * @n1: the value of the first number.
 * @n2: the value of the second number.
 *
 * Return: The result of adding these two numbers.
 */

int add(int n1, int n2)
{
int result;
result = n1 + n2;
return (result);
}
