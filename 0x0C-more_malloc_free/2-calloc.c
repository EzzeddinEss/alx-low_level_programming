#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _calloc - a function that allocates memory for an array, using malloc
 * @nmemb: the nubmer of bytes to be allocated
 * @size: the size of memory to be allocated
 *
 * Return: 0 if nmemb or size return null, and if it fails.
 */

void *_calloc(unsigned int nmemb, unsigned int size)
{
  unsigned int *ptr;
  
}
