#include "main.h"
/**
* swap_int - takes a pointer to an int and updates the value
* @a: the first integer
* @b: the second integer
* return: 0
*/

void swap_int(int *a, int *b)
{
	int x;

	x = *a;
	*a = *b;
	*b = m;

}
